<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <h2>hello <%= user ? user.profile.name : "friends" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img
        id="avatar"
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
  <% } %>
  <% if (!user) { %>
    <h4> Please log in using the Google button in the nav to check if your meal is vegan and/or share some of your tasty vegan snacks and meals.</h2>
    <h1>VeganRify🥑</h1>
  <% } %>
</main>

<%- include('./partials/footer') %>

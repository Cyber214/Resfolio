<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<div id="landing">
  <h2>hello <%= user ? user.profile.name : "friends" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img
        id="avatar"
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
    <h1>!!WELCOME!!</h1>
    <br>
    <h2>!To the prefect growth app for success!</h2>
    <h1>Resfolio</h1>
  <% } %>
  <% if (!user) { %>
    <h1>HEY!!!!</h1>
    <br>
    <h2>Don't search for jobs,</h2>
    <br>
    <h2>have employers search for you🫵🏼</h2>
    <h1>Resfolio</h1>
  <% } %>
  </div>

<%- include('./partials/footer') %>
